//todo: temp solution
import employers from '../assets/employers.json'

const markers = (state = employers, action) => {
  switch (action.type) {

    default:
      return state
  }
}

export default markers